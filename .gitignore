# Notification specific ignores
*.log
*.tmp
*.bak
# IMPORTANT: Never commit actual API keys or secrets here!
# These lines are examples, make sure your actual keys are not in the repo.
# secret_keys.txt
# api_credentials.env

# General ignores
.DS_Store
Thumbs.db

# vexp — internal files (auto-generated, not user-managed)
# index.db is rebuilt from manifest.json on checkout
.vexp/index.db
.vexp/index.db-wal
.vexp/index.db-shm
.vexp/vexp.log
.vexp/daemon.log
.vexp/daemon.sock
.vexp/daemon.pid
.vexp/daemon.pipe
.vexp/mcp.port
.vexp/healthy
.vexp/parent_workspace.json
.vexp/token-savings-report.md
.vexp/index.lock